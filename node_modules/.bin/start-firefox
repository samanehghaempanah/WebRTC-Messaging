#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  exec "$basedir/bash"  "$basedir/../travis-multirunner/start-firefox.sh" "$@"
else 
  exec bash  "$basedir/../travis-multirunner/start-firefox.sh" "$@"
fi
